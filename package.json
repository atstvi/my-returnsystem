{
  "name": "my-returnsystem",
  "version": "1.0.0",
  "private": true,
  "description": "Single-file return-system web app — sync stabilization test harness",
  "scripts": {
    "test": "node tests/syntax.test.js && node tests/entity-sync.test.js && node tests/merge.test.js && node tests/notion-merge.test.js && node tests/storage-report.test.js && node tests/storage-keyguard.test.js"
  }
}
