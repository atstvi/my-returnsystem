{
  "name": "return-widget",
  "version": "0.4.0",
  "private": true,
  "description": "Return PC desktop widget (Tauri v2). Separate buildable artifact — the web app stays single-file/no-build.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon src-tauri/icons/icon.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
